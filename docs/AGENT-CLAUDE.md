Claude Code

```
claude plugin marketplace add sliday/claude-plugins && claude plugin install tamp@sliday

ns-claude plugin marketplace add sliday/claude-plugins && ns-claude plugin install tamp@sliday
```